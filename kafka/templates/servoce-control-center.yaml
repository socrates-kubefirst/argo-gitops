apiVersion: v1
kind: Service
metadata:
  name: controlcenter
  namespace: {{ .Values.namespace }}
spec:
  type: {{ .Values.controlcenter.service.type }}
  ports:
    - port: 9021
      targetPort: 9021
  selector:
    app: {{ .Release.Name }}-controlcenter
